apply plugin: 'com.android.library'

android {
    compileSdkVersion rootProject.ndk19SupportLibMinSdk
    buildToolsVersion = rootProject.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ndk19SupportLibMinSdk
        ndk {
            moduleName "hello-jni"
        }
    }
}
