apply plugin: 'com.android.feature'

android {
    compileSdkVersion rootProject.latestCompileSdk
    buildToolsVersion = rootProject.buildToolsVersion
}

dependencies {
    featureSplit project(':feature')
    implementation project(':library')
}