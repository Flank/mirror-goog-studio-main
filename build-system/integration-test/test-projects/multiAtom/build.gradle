apply from: "../commonHeader.gradle"
buildscript { apply from: "../commonBuildScript.gradle" }

// Lint does not handle the split attribute properly.
subprojects {
    afterEvaluate {
        android {
            lintOptions {
                disable 'MissingPrefix'
            }
        }
    }
}
