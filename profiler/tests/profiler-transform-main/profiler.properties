android.profiler.okhttp.enabled=true
profiler.network.request.payload=true
# The production is disabling keyboard events by default and it can be
# overridden by the server flag. But we still test the functionality as
# it's enabled to avoid accidental breakage. See b/211154220.
android.profiler.keyboard.event.enabled=true
