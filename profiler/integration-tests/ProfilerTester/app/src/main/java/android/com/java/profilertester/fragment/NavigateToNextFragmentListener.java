package android.com.java.profilertester.fragment;

public interface NavigateToNextFragmentListener {
    /** When triggered, a handling activity should replace the current fragment with another one. */
    void onNavigateRequested();
}
