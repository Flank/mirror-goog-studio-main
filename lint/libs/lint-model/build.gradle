apply plugin: 'com.android.tools.java-library'
apply plugin: 'com.android.tools.kotlin'
apply plugin: 'com.android.tools.publish'
apply plugin: 'jacoco-tools-base'

group = 'com.android.tools.lint'
archivesBaseName = 'lint-model'
version = rootProject.ext.baseVersion

compileKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}

dependencies {
    compile project(':base:common')
    compile project(':base:sdk-common')
    compile project(':base:builder-model')
    compile libs.kotlin_stdlib
    compile libs.kxml
}

project.ext.pomName = 'Android Tools Lint Build System Model'
project.ext.pomDesc = 'Lint build system model'
